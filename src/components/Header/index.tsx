import MainNavigation from '../../components/MainNavigation';

const Header = () => {
  return (
    <header className="sticky inset-x-0 top-0 z-50 bg-primary">
      <MainNavigation />
    </header>
  );
};

export default Header;
